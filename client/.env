
# Server Port (already set in .replit but can be overridden here)
PORT=5000

# Database URL (required for production database connection)
# Currently using in-memory storage, but needed when switching to real database
DATABASE_URL=your_database_connection_string_here

# Node Environment
NODE_ENV=development
